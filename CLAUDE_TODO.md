# CLAUDE TODO

Bugs / cleanup ideas noticed out of scope. Triage and address when convenient.

## Open — cleanup

- **`ReplayEngine` default-stepper config trap (phase-4 review, 2026-07-10).** `ReplayEngine`'s no-arg default `stepper = SessionProgressionStepper()` uses default config even if `ReplayEngine(config = X)` is constructed with a non-default config — so `ExerciseBelief.seed/.override` (which use the engine's `config`) and the fold stepper could diverge. NOT a live bug: all three production call sites build `SessionProgressionStepper(config = config)` explicitly. Tidy by having `ReplayEngine` derive its default stepper from its own `config`. Found by the phase-4 whole-branch review.

- **Chart μ-drift parity (phase-2 follow-up, 2026-07-09).** `ExerciseProgressionSeriesBuilder` calls `MuscleStrengthProjector.project(...)` without `muscleLastObs`, so the exercise-detail chart's lines/dots age variance (q) but skip detraining μ-drift, while the planner path applies it. After a long layoff the chart line sits above the weight the planner will actually prescribe. Cosmetic; fix by threading the muscle clock into the series builder (it replays from scratch, so it can compute muscleLastObs) for chart/planner parity. Found by the phase-2 whole-branch review.

## Open — next phase

- **Phase 5 HARNESS SHIPPED 2026-07-10; ADOPTION CEREMONY PENDING (human-gated).** The offline forward-chaining CV harness (`RecalibrationHarness`, test tree, analysis-only) is built and ran on the real 24-session history (spec `docs/superpowers/specs/2026-07-10-phase5-defaults-from-data-design.md`, plan `.../plans/2026-07-10-phase5-defaults-from-data.md`). It re-derived the n=1 gate as "use what we have honestly" (user decision: n=1 > n=0). Result (16 folds, CV +31.3 vs defaults): **procNoise ×16 PINS_BOUND** (default 8e-5/day too low — pins even the widened ÷16…×16 box, so true value is beyond one history; adopt a conservative bump ×4–×8 AND widen the production fitter's ÷4…×4 bound), **tau ×2.56 STABLE** (but a monotonic climb 1.2→2.5 — may not have plateaued; adopt with shrinkage, ~×2), **fatigue ×0.149** (flagged FRAGILE but that's a relative-IQR artifact on small multipliers — mature trajectory is absolutely tight ~0.12–0.19 and directionally wants much lower φ; look at the trajectory, consider a moderate reduction), **drift ×0.99** (no signal, leave at default). Report artifact regenerated by running `RecalibrationReportTest` (writes `app/build/recalibration-report.txt`).
  - **REMAINING (the human-gated adoption ceremony, spec §4), per param you choose to adopt:** edit the constant in `EstimatorConfig` → re-pin `BeliefSimulationTest` → re-baseline `BacktestComparisonTest` default-config baseline → `ProdBssPrescriptionTest` sanity check (20 lb) → version bump → add a "Phase 5 — defaults from data" section to `docs/adaptation/06-fitting.md`. Separate decision: widen the production per-user fitter's ÷4…×4 bounds for any PINS_BOUND param (procNoise).
  - **Harness quality nits (non-blocking, from final review):** `classify` double-sorts the mature half; the relative-IQR flag metric mis-reads monotonic climbs as STABLE and absolutely-tight small multipliers as FRAGILE (advisory only — read the trajectory, not just the flag); `classify(emptyList())`/`assemble(emptyRows)` edge paths untested. τ is nearly inert in the *synthetic* unit tests (single-exercise muscle) — only the real multi-sibling fixture exercises it.
  - **Still genuinely blocked on n>1** for the population-level empirical-Bayes claim (n=1 personalizes = training-data circularity for "population truth"); the harness takes `List<UserHistory>` so user #2 slots in with no structural change.

## Open — intended / accepted-by-design (no action needed, kept for visibility)

